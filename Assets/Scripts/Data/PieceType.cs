namespace Rotatris.Data
{
    /// <summary>
    /// The seven standard tetromino shapes, plus a special Core marker used
    /// for the single fixed cell the structure grows from. 
    /// </summary>
    public enum PieceType { I, J, L, O, S, T, Z, Core }
}